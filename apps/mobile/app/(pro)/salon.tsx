import React from 'react';
import { Step4Address } from '@/screens/pro/Step4Address';

/** Profil → Adresse et zone (étape 4 réutilisée en réglage). */
export default function SalonAddress() {
  return <Step4Address settings />;
}
