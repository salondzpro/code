import React from 'react';
import { Step8Works } from '@/screens/pro/Step8Works';

/** Mon salon → Réalisations (étape 8 réutilisée en réglage). */
export default function WorksSettings() {
  return <Step8Works settings />;
}
