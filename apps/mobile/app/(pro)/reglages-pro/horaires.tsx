import React from 'react';
import { Step9Hours } from '@/screens/pro/Step9Hours';

/** Profil → Horaires (étape 9 réutilisée en réglage). */
export default function HoursSettings() {
  return <Step9Hours settings />;
}
